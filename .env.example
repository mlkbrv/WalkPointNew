# Values compose itself reads. Per-service secrets live in backend/.env.
# Copy to .env and fill in before the first `docker compose up`.

POSTGRES_DB=stride
POSTGRES_USER=stride
POSTGRES_PASSWORD=

# Gunicorn workers. A good starting point is (2 x CPU cores) + 1.
WEB_CONCURRENCY=3
