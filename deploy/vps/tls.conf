# Overwritten by bootstrap.sh.
#
# Empty means Caddy manages a public certificate automatically, which is what
# you want whenever this stack owns :80. bootstrap.sh replaces the file with
# `tls internal` when the ports had to move, because ACME's HTTP-01 challenge is
# only ever answered on :80.
