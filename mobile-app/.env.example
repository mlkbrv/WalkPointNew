# Gemini (existing AI Studio integration)
GEMINI_API_KEY=

# API base URL.
# Leave empty for the web dev server: vite proxies /v1 to http://localhost:8000.
# For a device build point it at the reachable API origin, e.g. http://192.168.1.10:8000
EXPO_PUBLIC_API_BASE_URL=
