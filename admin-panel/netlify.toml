[build]
  command = "npm run build"
  publish = "dist"

# VITE_API_BASE_URL must be set in the site's environment — it is baked into the
# bundle at build time, not read at runtime.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
